[ ] Create Interstate Geography Levels

  
  [x] Table definitions state -> states
    Alter schema & update fields

    [x] top_level_travel_time_reliability
    [x] top_level_freight_reliability
    [x] top_level_total_excessive_delay
    [x] urban_area_populations

    see: ./top_level_total_excessive_delay-StateColumnToStatesArray.sh

  [x] Urban Area Populations
    [x] Interstate Census UA scraper
      [x] States column as postgres array

    [x] Alter urban_area_populations table
      [x] Add states column

    [x] Loader for Urban Area Populations CSVs

    [x] Fix dependents of urban_area_population
      [ ] Find
        [x] in database
          sql/geography_level_attributes_view/createStateGeographyAttributesView.sql
        [x] in api server
          NONE
      [x] Fix
        [x] in database
        [x] in api server

  [x] Alter geography_level_attributes_view
    [x] Alter view definition
      [x] states column
        [x] rename to states
        [x] change type to array
      [x] provide geography level attributes broken down by
        [x] individual state
        [x] interstate
          
    [ ] Fix dependents of geography_level_attributes_view
      [x] Find
        [x] in database
          * create_state_top_level_travel_time_reliability_yrmo.sql
        [x] in api server
          * DAOs/MetadataDAO/geographyLevelAttributes.js
      [ ] Fix
        [ ] in database
        [x] in api server

    [ ] top_level measure tables state -> states
      [ ] top_level_travel_time_reliability
        [x] transform scripts
        [ ] table definition
      [ ] top_level_freight_reliability
      [ ] top_level_total_excessive_delay 

LATER:

  [ ] Load PA
    [ ] shapefile
    [ ] data

  [ ] Look into optimizing top_level measure builds
    Doesn't seem to be using constraint.

			npmrds_local=# \d ny.lottr_percentiles
				 Table "ny.lottr_percentiles"
			 Column |         Type         | Modifiers
			--------+----------------------+-----------
			 state  | character varying(2) |
			 tmc    | character varying(9) |
			 year   | smallint             |
			 month  | smallint             |
			 data   | jsonb                |
			Check constraints:
					"state_check" CHECK (state::text = 'ny'::text)
			Inherits: lottr_percentiles
			Number of child tables: 37 (Use \d+ to list them.)

		Would changing the type of state in the check from text to VARCHAR(2) help?
 


NOTES:
  urban_area_populations dependents:
    npmrds_local=# drop table urban_area_populations ;
    ERROR:  cannot drop table urban_area_populations because other objects depend on it
    DETAIL:  materialized view geography_level_attributes_view depends on table urban_area_populations
    table us.urban_area_populations_y2015 depends on table urban_area_populations
    table us.urban_area_populations_y2016 depends on table urban_area_populations
    HINT:  Use DROP ... CASCADE to drop the dependent objects too.

SCRIPTS ORDER:
  1. top_level_total_excessive_delay-StateColumnToStatesArray.sh
  2. update-urban-areas-table-def.sh
